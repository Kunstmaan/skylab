steps:
  - ssh %deploy_server% -p %deploy_port% "sudo skylab maintenance --quick || sudo python /opt/kDeploy/tools/maintenance.py --quick"
  - ssh %deploy_server% -p %deploy_port% "if ps ax | grep -v grep | grep 'nginx' > /dev/null; then sudo service nginx reload; else sudo service apache2 reload; fi"
